import React from 'react';
import classNames from 'classnames/bind';
import styles from 'css/main';

const cx = classNames.bind(styles);

const Footer = () => (
  <div className={cx("dash-footer")}>Footer</div>
)

export default Footer;